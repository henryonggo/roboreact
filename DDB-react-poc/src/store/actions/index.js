import * as miscImport from "./misc";
import * as themeImport from "./theme";
import * as tabImport from "./tab";
import * as widgetImport from "./widget";
import * as generalImport from "./general";

export const misc = miscImport;
export const theme = themeImport;
export const tab = tabImport;
export const widget = widgetImport;
export const general = generalImport;

export default {
    misc, 
    theme,
    tab,
    widget,
    general,
};