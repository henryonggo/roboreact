import WIDGET_BASE_TYPES from "constants/widgets/widgetBaseTypes";

export const marginalRenderStyles = {
    [WIDGET_BASE_TYPES.MARGINALS_WIDGET]: {
        maxHeight: "auto",
        height: "auto"
    }
}

export default {
    marginalRenderStyles
};